import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';
import "generated/i18n.dart";
import 'dart:convert';

import'webSocket.dart';

import 'playerClass.dart';
import 'taskClass.dart';

Widget taskViewFourthRow(BuildContext context, Player firstPlayer, Player secondPlayer, Task task) {
  if(1==1) {
    return Row(
      children: <Widget>[
        Container()
      ],
    );
  }
    else  {
      return Container();
  }
}